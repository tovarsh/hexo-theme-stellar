<% if (page.layout === 'post' || (page.wiki?.length > 0)) { %>
  <script>
    let tianliGPT_postSelector = 'article.content';
    let tianliGPT_key = '<%= theme.plugins.tianli_gpt.api %>';
    let tianliGPT_typingAnimate = '<%= theme.plugins.tianli_gpt.typingAnimate %>';
    var tianli_name = '<%= theme.plugins.tianli_gpt.name %>';
    var tianli_text = '<%= theme.plugins.tianli_gpt.text %>';
    var tianli_icon_url = '<%= theme.plugins.tianli_gpt.icon %>';
  </script>
  <script defer src="<%= theme.plugins.tianli_gpt.cdn %>"></script>
<% } %>
